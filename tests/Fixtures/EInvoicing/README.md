# TEST-ONLY cryptographic fixtures

These keys and certificates are **not** ZATCA CSIDs and are **not** a production identity.

They exist only so Phase 9 can parse X.509 metadata and exercise a local test signer.
Do not copy them into application configuration, migrations, or production secret storage.
